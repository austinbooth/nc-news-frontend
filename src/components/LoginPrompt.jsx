import React from "react";

const LoginPrompt = () => {
  return (
    <section className="login-prompt">
      <h2>You need to be logged in to vote</h2>
      <p>Please login using the button at the top</p>
    </section>
  );
};

export default LoginPrompt;
